export const loadEntries = async (/*{ commit, state }*/) => {
  // Do something
}

export const updateEntry = async (/*{ commit, state }*/) => {
  // Do something
}

export const createEntry = async (/*{ commit, state }*/) => {
  // Do something
}